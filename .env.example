# Example env file. Do NOT commit real credentials to the repo.
# Copy to .env and set real values locally or in Render environment variables.

# SECRET_KEY=your-secret-key
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# RENDER_EXTERNAL_HOSTNAME=your-service.onrender.com
# SECRET_KEY must be set on production (Render) and DATABASE_URL should be set only in production.
